# 首页功能改造计划

## 需求概述

根据提供的三张参考图片，需要改造首页功能，主要包括：

1. **使用真实持仓股票数据** - 替换当前的模拟数据
2. **交易时间段展示** - 图表时间轴需要显示：9:15-9:30, 9:30-11:30, 13:00-15:00
3. **持仓股票列表** - 页面底部展示每个股票的详细信息

## 参考图片分析

### 图片1：持仓股票列表界面
- 显示账户总资产、浮动盈亏、当日参考盈亏
- 持仓股票列表包含：市值、盈亏、持仓/可用、成本/现价
- 股票信息包括：创元科技、智慧农业、华菱线缆、北斗星通等

### 图片2：分时图界面
- 显示股票的分时走势图
- 包含均价线和最新价格线
- 时间轴显示：09:30, 11:30, 15:00
- 显示均价、最新价、涨跌幅等信息

### 图片3：自选股列表界面
- 显示股票列表，包含涨跌幅、价格
- 每个股票有迷你K线图
- 支持多个股票同时展示

## 实施计划

### 阶段1：数据层改造

#### 1.1 创建持仓数据API
**文件**: `src/app/api/portfolio/holdings/route.ts`
- 从数据库获取当前持仓股票数据
- 调用股票API获取实时价格
- 计算每个股票的市值、盈亏、盈亏率
- 返回格式化的持仓数据

**数据结构**:
```typescript
interface HoldingStock {
  code: string;           // 股票代码
  name: string;          // 股票名称
  quantity: number;      // 持仓数量
  available: number;     // 可用数量
  costPrice: number;     // 成本价
  currentPrice: number;  // 现价
  marketValue: number;   // 市值
  profit: number;        // 盈亏
  profitRate: number;    // 盈亏率
}
```

#### 1.2 创建分时数据API
**文件**: `src/app/api/stock/intraday/route.ts`
- 获取单只股票的分时数据
- 支持交易时间段：9:15-9:30, 9:30-11:30, 13:00-15:00
- 返回时间点和对应的价格、均价

### 阶段2：组件开发

#### 2.1 持仓股票列表组件
**文件**: `src/components/holdings/HoldingsList.tsx`
- 展示持仓股票列表
- 显示：市值、盈亏、持仓/可用、成本/现价
- 支持排序功能
- 参考图片1的样式

#### 2.2 分时图组件
**文件**: `src/components/charts/IntradayChart.tsx`
- 使用ECharts绘制分时图
- 显示价格线和均价线
- 时间轴显示：09:30, 11:30, 15:00
- 支持点击查看详情
- 参考图片2的样式

#### 2.3 账户摘要组件
**文件**: `src/components/holdings/AccountSummary.tsx`
- 显示总资产、浮动盈亏、当日参考盈亏
- 显示可用资金、可取资金
- 参考图片1的顶部区域

### 阶段3：Hook开发

#### 3.1 持仓数据Hook
**文件**: `src/hooks/useHoldings.ts`
- 获取持仓股票数据
- 定时刷新实时价格
- 计算总资产、总盈亏等

#### 3.2 分时数据Hook
**文件**: `src/hooks/useIntradayData.ts`
- 获取单只股票的分时数据
- 处理交易时间段过滤
- 支持实时更新

### 阶段4：首页改造

#### 4.1 修改首页布局
**文件**: `src/app/page.tsx`
- **移除顶部导航栏和状态栏**（保持原有页面头部）
- **移除底部导航栏**
- 主体内容区域：
  - 顶部：账户摘要（总资产、盈亏等）
  - 中间：分时图（可选，或保留原有资产价值图表）
  - 底部：持仓股票列表
- **保持亮色主题**（白色背景，与原有设计一致）

#### 4.2 时间轴处理
- 修改图表组件，支持交易时间段显示
- 过滤非交易时间段的数据点
- 时间轴标签：09:15, 09:30, 11:30, 13:00, 15:00

### 阶段5：样式优化

#### 5.1 亮色主题适配
- **保持亮色主题**（白色背景 `#fff`，浅灰背景 `#f5f5f5`）
- 调整颜色方案：
  - 红色（`#ef4444`）表示上涨/盈利
  - 绿色（`#10b981`）表示下跌/亏损
  - 文字颜色：深灰（`#333`）为主，浅灰（`#666`, `#999`）为辅
- 边框颜色：浅灰（`#e5e7eb`）

#### 5.2 响应式布局
- 确保移动端和桌面端都能正常显示
- 卡片式设计，使用圆角和阴影增强层次感

## 技术要点

### 交易时间段处理
```typescript
// 交易时间段
const TRADING_PERIODS = [
  { start: '09:15', end: '09:30' },  // 集合竞价
  { start: '09:30', end: '11:30' },  // 上午交易
  { start: '13:00', end: '15:00' },   // 下午交易
];

// 过滤数据点
function filterTradingTime(dataPoints: DataPoint[]): DataPoint[] {
  return dataPoints.filter(point => {
    const time = new Date(point.timestamp);
    const hour = time.getHours();
    const minute = time.getMinutes();
    const timeStr = `${String(hour).padStart(2, '0')}:${String(minute).padStart(2, '0')}`;
    
    return TRADING_PERIODS.some(period => 
      timeStr >= period.start && timeStr <= period.end
    );
  });
}
```

### 持仓数据计算
```typescript
// 计算持仓信息
function calculateHolding(
  quantity: number,
  costPrice: number,
  currentPrice: number
) {
  const marketValue = quantity * currentPrice;
  const profit = (currentPrice - costPrice) * quantity;
  const profitRate = ((currentPrice - costPrice) / costPrice) * 100;
  
  return { marketValue, profit, profitRate };
}
```

## 文件清单

### 新建文件
1. `src/app/api/portfolio/holdings/route.ts` - 持仓数据API
2. `src/app/api/stock/intraday/route.ts` - 分时数据API
3. `src/components/holdings/HoldingsList.tsx` - 持仓列表组件
4. `src/components/holdings/AccountSummary.tsx` - 账户摘要组件
5. `src/components/charts/IntradayChart.tsx` - 分时图组件
6. `src/hooks/useHoldings.ts` - 持仓数据Hook
7. `src/hooks/useIntradayData.ts` - 分时数据Hook

### 修改文件
1. `src/app/page.tsx` - 首页布局改造
2. `src/components/charts/PortfolioChart.tsx` - 支持交易时间段
3. `src/lib/data/sina-api.ts` - 可能需要添加分时数据接口

## 开发顺序

1. ✅ 创建计划文档（当前步骤）
2. 创建持仓数据API
3. 创建持仓数据Hook
4. 创建持仓列表组件
5. 创建账户摘要组件
6. 修改首页集成新组件
7. 创建分时图组件（可选）
8. 样式优化和测试

## 设计变更说明

### UI调整
- ✅ **移除顶部区域**：不包含状态栏、头部导航、交易操作栏
- ✅ **移除底部导航**：不包含底部导航栏
- ✅ **亮色主题**：保持原有页面的亮色设计风格
  - 背景色：`#f5f5f5`（页面背景）、`#fff`（卡片背景）
  - 文字色：`#333`（主要文字）、`#666`（次要文字）、`#999`（辅助文字）
  - 边框色：`#e5e7eb`
  - 涨跌色：红色 `#ef4444`（上涨）、绿色 `#10b981`（下跌）

### 布局结构
```
┌─────────────────────────────┐
│   账户摘要区域（白色卡片）    │
│  - 总资产、浮动盈亏等        │
└─────────────────────────────┘
┌─────────────────────────────┐
│   图表区域（白色卡片）        │
│  - 分时图/资产价值图          │
└─────────────────────────────┘
┌─────────────────────────────┐
│   持仓股票列表（白色卡片）    │
│  - 股票详细信息              │
└─────────────────────────────┘
```

## 注意事项

1. **数据源**：需要确认持仓数据是从数据库获取还是从交易引擎获取
2. **实时更新**：考虑使用WebSocket或定时轮询更新价格
3. **性能优化**：批量获取股票价格，避免频繁请求
4. **错误处理**：处理股票数据获取失败的情况
5. **缓存策略**：合理使用缓存减少API调用
6. **主题一致性**：保持与原有页面（`src/app/page.tsx`）的亮色主题一致

