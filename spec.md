# A股 AI 交易模拟平台 - 产品规格说明书

> 版本: 1.0
> 更新日期: 2025-02-04
> 状态: 需求确认中

---

## 1. 产品概述

### 1.1 产品定位
一个面向个人用户的 AI 驱动 A 股模拟交易平台，多个 AI 代理独立运作，实时盯盘并自主交易决策。

### 1.2 核心价值
1. **观察 AI 交易风格差异**: 对比不同大模型（DeepSeek、Gemini 等）的交易行为
2. **辅助交易决策**: 参考 AI 分析思路，辅助个人投资判断
3. **量化学习沙盒**: 学习量化交易逻辑，验证交易策略

### 1.3 目标用户
- 主要：个人投资者（单用户，需登录）
- 次要：量化交易学习者

---

## 2. 功能需求

### 2.1 用户认证
| 功能 | 说明 |
|------|------|
| 登录方式 | 密码登录（无需第三方） |
| 用户数据隔离 | 每个用户独立的 AI 配置、股票池、交易历史 |
| 数据库 | 用户记录存储在 `users` 表 |

### 2.2 AI 代理系统

#### 2.2.1 内置模型
| 模型 | OpenRouter ID | 说明 |
|------|---------------|------|
| DeepSeek | `deepseek/deepseek-chat` | 主要模型 |
| Gemini 2.0 Flash | `google/gemini-2.0-flash-exp` | 快速响应 |

#### 2.2.2 自定义 AI
- 用户可通过 OpenRouter API 添加任意模型
- 配置项：模型名称、模型 ID、API Key（可选）

#### 2.2.3 决策机制
| 参数 | 说明 |
|------|------|
| 思考间隔 | 1-60 秒可调（全局配置） |
| 决策依据 | 新浪 API 提供的全部数据 |
| 无操作选项 | AI 可选择 `hold`（不做任何操作） |
| 启停控制 | 全局开关 + 单个 AI 开关 |

#### 2.2.4 数据输入（按优先级）
1. **实时行情**（最高优先级）：当前价、买卖五档、成交量
2. **分时数据**：分钟级价格/成交量
3. **日 K 线**：历史 OHLCV
4. **个股资料**：市盈率、市净率、市值

#### 2.2.5 决策输出
```typescript
{
  action: 'buy' | 'sell' | 'hold',
  stock?: string,      // 股票代码
  quantity?: number,   // 数量
  reason: string       // 决策理由
}
```

### 2.3 交易规则

#### 2.3.1 基础规则
| 规则 | 说明 |
|------|------|
| 初始资金 | 每个 AI ¥100,000 |
| T+1 | 当日买入的股票，次日才能卖出 |
| 交易时间 | 9:15-9:30（集合竞价）、9:30-11:30、13:00-15:00 |
| 最小单位 | 100 股（1 手） |

#### 2.3.2 风控规则
| 规则 | 限制 |
|------|------|
| 单票持仓上限 | 总资金的 50% |
| 涨停处理 | 买入失败，AI 需感知 |
| 跌停处理 | 卖出失败，AI 需感知 |
| 停牌处理 | 持仓冻结，无法交易 |

#### 2.3.3 做 T 规则
- AI 自主决定是否建底仓做 T
- 无次数限制，由 AI 自行把握时机
- 支持低吸高抛、高抛低吸等多种模式

### 2.4 股票池管理

#### 2.4.1 添加方式
1. **手动输入**: 输入股票代码（如 `sh600519`）
2. **搜索**: 通过股票名称/代码搜索（需接入股票列表 API）

#### 2.4.2 管理页面展示
| 信息 | 说明 |
|------|------|
| 池内列表 | 股票代码、名称、实时价格、涨跌幅 |
| 持仓分布 | 显示哪些 AI 持有该股票 |

### 2.5 首页大屏（Dashboard）

#### 2.5.1 布局结构
```
+----------------------------------------------------------+
|  Header: 导航栏                                            |
+----------------------------------------------------------+
|  主区域: 各 AI 资产曲线图（多线对比）                        |
+----------------------------------------------------------+
|  副区域: 股票实时分时图                                    |
+----------------------------------------------------------+
|  AI 卡片区: 每个 AI 的详细状态                              |
|  ┌─────────┐ ┌─────────┐ ┌─────────┐                      |
|  │DeepSeek │ │ Gemini  │ │  Claude │                      |
|  └─────────┘ └─────────┘ └─────────┘                      |
+----------------------------------------------------------+
|  底部统计: 今日操作次数、胜率、最大回撤                      |
+----------------------------------------------------------+
```

#### 2.5.2 AI 卡片内容
| 指标 | 说明 |
|------|------|
| 总资产 | 现金 + 持仓市值 |
| 今日盈亏 | 金额 & 百分比 |
| 累计盈亏 | 金额 & 百分比 |
| 当前持仓 | 股票名、数量、成本价、当前价 |
| 排序 | 按收益率排序 |

#### 2.5.3 底部统计
| 指标 | 说明 |
|------|------|
| 今日操作次数 | 每个 AI 的交易次数 |
| 胜率 | 盈利交易次数 / 总交易次数 |
| 最大回撤 | 历史最大资产回撤幅度 |

### 2.6 群聊窗口（AI 思考流）

#### 2.6.1 消息类型
| 类型 | 展示样式 |
|------|----------|
| 思考过程 | 灰色，可折叠 |
| 决策结果 | 高亮显示 |
| 执行状态 | 成功（绿）/失败（红） |

#### 2.6.2 消息密度控制
- 可折叠"无操作"的思考周期
- 用户可手动暂停滚动
- 支持按 AI 过滤消息

### 2.7 交易记录

#### 2.7.1 记录字段
| 字段 | 说明 |
|------|------|
| 时间 | 精确到秒 |
| AI | 哪个模型 |
| 股票 | 代码和名称 |
| 方向 | 买入 / 卖出 |
| 数量 | 股数 |
| 价格 | 成交价 |
| 原因 | AI 的决策理由 |

#### 2.7.2 持久化
- 每笔交易立即写入数据库
- 每分钟保存资产快照
- 存储表：`trades`、`account_snapshots`

### 2.8 K 线回放

#### 2.8.1 功能
- 支持指定任意日期回放
- 回放期间显示 AI 的思考和操作
- 可暂停/继续/拖动进度

#### 2.8.2 数据来源
- 新浪历史 K 线 API
- 本地数据库的历史快照

### 2.9 排行榜

#### 2.9.1 排行维度
| 排行榜 | 排序依据 |
|--------|----------|
| 收益榜 | 累计收益率 |
| 今日榜 | 今日盈亏 |
| 胜率榜 | 交易胜率 |
| 回撤榜 | 最大回撤（升序） |

---

## 3. 技术架构

### 3.1 技术栈
| 层级 | 技术 |
|------|------|
| 前端 | Next.js 16 + React 19 + TypeScript |
| 样式 | Tailwind CSS |
| 图表 | ECharts |
| 数据库 | PostgreSQL (Supabase) |
| ORM | Drizzle ORM |
| AI 服务 | OpenRouter API |

### 3.2 数据流架构
```
┌─────────────┬─────────────┬──────────────┐
│  前端页面   │   API路由   │  服务层      │
│  (Next.js)  │  (/api/*)   │  (lib/*)     │
└─────────────┴─────────────┴──────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        ▼                       ▼                       ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ 数据服务层    │    │ AI 决策层     │    │ 交易引擎层    │
│ sina-api.ts   │    │ decision.ts   │    │ engine.ts     │
│ cache.ts      │    │ prompts.ts    │    │ rules.ts      │
└───────────────┘    └───────────────┘    └───────────────┘
        │                       │                       │
        ▼                       ▼                       ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ 新浪财经 API  │    │ OpenRouter    │    │ Drizzle ORM   │
│ + 缓存层      │    │ (多模型)      │    │ + PostgreSQL  │
└───────────────┘    └───────────────┘    └───────────────┘
```

### 3.3 核心模块

#### 3.3.1 数据服务（`lib/data/`）
| 模块 | 功能 |
|------|------|
| `sina-api.ts` | 新浪 API 封装，支持实时行情、分时、K 线 |
| `cache.ts` | 内存缓存 + 请求合并，防止限流 |

#### 3.3.2 AI 服务（`lib/ai/`）
| 模块 | 功能 |
|------|------|
| `client.ts` | OpenRouter 客户端配置 |
| `schema.ts` | Zod 决策输出 schema |
| `prompts.ts` | 提示词模板 |
| `decision.ts` | 决策生成服务 |

#### 3.3.3 交易引擎（`lib/trading/`）
| 模块 | 功能 |
|------|------|
| `rules.ts` | T+1、涨跌停、停牌规则校验 |
| `engine.ts` | 交易执行核心逻辑 |
| `account.ts` | 账户管理（持仓、资金） |

#### 3.3.4 实盘管理（`lib/live-trading/`）
| 模块 | 功能 |
|------|------|
| `manager.ts` | 多 AI 协调调度器 |
| `scheduler.ts` | 定时任务（1-60s 可调） |

### 3.4 数据库 Schema（现有）

| 表名 | 用途 |
|------|------|
| `users` | 用户信息 |
| `trades` | 交易记录 |
| `positions` | 持仓快照 |
| `ai_decisions` | AI 决策记录 |
| `account_snapshots` | 账户快照 |
| `live_trading_status` | 实盘交易状态 |
| `portfolio_series_points` | 资产曲线点位 |
| `stock_intraday_points` | 股票分时点位 |
| `portfolio_events` | 事件记录（回放用） |

### 3.5 API 路由设计

| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/logout` | POST | 用户登出 |
| `/api/auth/session` | GET | 获取当前会话 |
| `/api/stock/data` | GET | 获取股票数据 |
| `/api/stock/search` | GET | 搜索股票 |
| `/api/ai/decision` | POST | 获取 AI 决策 |
| `/api/portfolio/value` | GET | 获取组合价值 |
| `/api/portfolio/intraday` | GET | 获取分时数据 |
| `/api/portfolio/realtime` | GET | 获取实时数据 |
| `/api/live/start` | POST | 启动实盘交易 |
| `/api/live/stop` | POST | 停止实盘交易 |
| `/api/live/status` | GET | 获取交易状态 |
| `/api/history/trades` | GET | 获取交易历史 |
| `/api/history/decisions` | GET | 获取决策历史 |
| `/api/history/positions` | GET | 获取持仓历史 |
| `/api/replay/start` | POST | 启动 K 线回放 |
| `/api/replay/data` | GET | 获取回放数据 |
| `/api/leaderboard` | GET | 获取排行榜 |

---

## 4. 边缘场景处理

### 4.1 市场状态
| 场景 | 处理 |
|------|------|
| 非交易时间 | AI 暂停决策，显示"市场休市" |
| 节假日 | 从交易日历 API 获取，自动跳过 |
| 半日交易 | 识别特殊交易日，调整结束时间 |

### 4.2 数据异常
| 场景 | 处理 |
|------|------|
| API 超时 | 降级到缓存数据，标记"数据延迟" |
| 数据缺失 | 跳过该决策周期，记录错误日志 |
| 异常价格 | 涨跌停检测，过滤不可能的价格 |

### 4.3 交易异常
| 场景 | 处理 |
|------|------|
| 资金不足 | 交易失败，AI 感知，下次调整 |
| 持仓不足 | 卖出失败，AI 感知 |
| 涨停买入失败 | 记录"买入失败（涨停）" |
| 跌停卖出失败 | 记录"卖出失败（跌停）" |

### 4.4 AI 异常
| 场景 | 处理 |
|------|------|
| API 调用失败 | 重试 3 次，失败后跳过该周期 |
| 超时 | 30s 超时，跳过该周期 |
| 输出格式错误 | 使用 Zod 校验，失败后要求重新生成 |
| 成本控制 | 记录调用次数，支持设置配额 |

---

## 5. UI/UX 设计要点

### 5.1 配色方案
| 用途 | 颜色 |
|------|------|
| 上涨（盈） | 红色 `#ef4444` |
| 下跌（亏） | 绿色 `#22c55e` |
| 主品牌 | 蓝紫渐变 |
| 背景 | 白色/浅灰 |

### 5.2 响应式设计
- 桌面端优先（大屏场景）
- 平板适配（两列布局）
- 移动端基础支持（单列）

### 5.3 加载与错误状态
| 场景 | 展示 |
|------|------|
| 初始加载 | 骨架屏 |
| 数据刷新 | 局部加载指示器 |
| 错误 | 友好提示 + 重试按钮 |

---

## 6. 部署与运维

### 6.1 部署环境
| 环境 | 平台 |
|------|------|
| 生产 | Vercel |
| 数据库 | Supabase Cloud |

### 6.2 环境变量
```env
OPENROUTER_API_KEY=xxx
DATABASE_URL=postgresql://...
NEXT_PUBLIC_SINA_API_BASE=https://hq.sinajs.cn
```

### 6.3 监控指标
- API 调用成功率
- AI 决策延迟
- 数据刷新延迟
- 错误日志聚合

---

## 7. 待确认事项

### 7.1 需要确认
- [ ] 股票列表 API 来源（新浪是否有？需第三方？）
- [ ] 交易日历数据来源
- [ ] AI 调用成本预算上限
- [ ] 历史数据保留时长

### 7.2 后续迭代
- [ ] 策略回测功能
- [ ] 自定义指标支持
- [ ] 多用户支持
- [ ] 移动端优化

---

## 附录：用户故事

### US1: 用户登录并查看 AI 状态
> 作为用户，我希望登录后能立即看到所有 AI 的当前状态，包括总资产、盈亏和持仓。

### US2: 启动 AI 实盘盯盘
> 作为用户，我希望启动实盘交易后，AI 能每 10 秒思考一次并自主决策。

### US3: 观察 AI 思考过程
> 作为用户，我希望在群聊窗口看到 AI 的思考过程和决策理由。

### US4: 查看 AI 交易记录
> 作为用户，我希望查看每个 AI 的所有交易记录，包括时间、股票、价格和原因。

### US5: 回放历史 K 线
> 作为用户，我希望选择任意日期回放当时的行情和 AI 决策。

### US6: 管理股票池
> 作为用户，我希望添加或移除股票池中的股票，并看到哪些 AI 持有它们。

### US7: 添加自定义 AI
> 作为用户，我希望添加自己的 OpenRouter 模型来参与交易。
